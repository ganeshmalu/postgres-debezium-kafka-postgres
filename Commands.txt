docker-compose up -d
docker-compose down

curl -X POST -H "Content-Type: application/json" --data @postgres-connector.json http://localhost:8083/connectors
curl -X POST -H "Content-Type: application/json" --data @postgres-sink.json http://localhost:8083/connectors

# Delete the connector
curl -X PUT http://localhost:8083/connectors/postgres-audit-sink/pause
curl -X PUT http://localhost:8083/connectors/postgres-source/pause

# Pause the connector
curl -X DELETE http://localhost:8083/connectors/postgres-source
curl -X DELETE http://localhost:8083/connectors/postgres-audit-sink

# Check connector status
curl http://localhost:8083/connectors/postgres-source/status
curl http://localhost:8083/connectors/postgres-audit-sink/status

docker exec -it postgres psql -U debezium -d sourceDBPostgres

\dn               -- list schemas
\dt cep_core.*      -- list tables in schema 'cep_core'
\dt cep_core_audit.*  -- list tables in schema 'cep_core_audit'


INSERT INTO cep_core.customer (name, email)
VALUES ('Alice', 'alice@example.com'),
       ('Bob', 'bob@example.com');

INSERT INTO cep_core.orders (customer_id, amount, status)
VALUES (1, 199.99, 'NEW'),
       (2, 299.50, 'PENDING');
